## 오늘 한 일
📃 **나만의 사전 만들기**
 - **게시글 목록을 화면에 그리기 (각각 뷰는 카드 뷰로 만들기)**
 - **게시글 내의 예시는 파란 글씨로 보여주기**
 
① **레이아웃 잡기** : 큰 틀 -> 카드 틀(단어 + 아이콘버튼 3개, 의미, 예시) 
 
② **리덕스 스토어에서 데이터 가져오기** : _useSelector_
```javascript
import { useSelector } from "react-redux";
const data = useSelector((state) => state.words.list);
```
 
③ **가져온 데이터를 map()을 사용해서 하나씩 출력하기**
```javascript
 {data.map ((list, index) => {
     return (
        <CardBox>
       		<Word>{list.word}</Word>
            <Mean>{list.mean}</Mean>
            <Ex>{list.ex}</Ex>
       </CardBox>
```
④ **Ex의 styled-componentsd에서 글씨색 바꾸기**
```javascript
const Ex = styled.div`
    margin-bottom : 20px;
    font-size : 20px;
    font-family: 'Do Hyeon', sans-serif;
    color : #7FB5FF;
`;
```
 ⭐ **전체 코드** ⭐
```javascript
import React, {useState, useEffect} from "react";
import { useHistory } from "react-router-dom";
import { useSelector } from "react-redux";
import styled from "styled-components";
import { BsCheckLg,BsPencilSquare,BsTrash } from "react-icons/bs";

const WordCard = (props) => {
    const history = useHistory();

    const data = useSelector((state) => state.words.list);

    const [isClicked, setIsCilcked] = useState(false);
    const onClick = () => {
        setIsCilcked(!isClicked); 
      };

    return (
        <Cards>
            {data.map ((list, index) => {
                return (
                    <CardBox key={index} id={list.id} >
                        <Word>{list.word}</Word>
                            <button className="btn_check" onClick={onClick}><BsCheckLg size="25"/></button>
                            <button className="btn_edit" onClick={() => { history.push('/Edit');}}><BsPencilSquare size="25"/></button>
                            <button className="btn_delete" onClick={() => { alert("삭제했습니다!");}}><BsTrash size="25"/></button>
                        <Mean>{list.mean}</Mean>
                        <Ex>{list.ex}</Ex>
                    </CardBox>
                )
        })}
        </Cards>
    )
}
```
<br/>

 - **게시글 작성에 필요한 input 3개를 ref로 관리하기**
 
① **input 박스에서 텍스트를 가지고 와야 한다 -> React 요소를 가지고 오기** : _React.useRef()_

```javascript
    const word = React.useRef(null);
    const mean = React.useRef(null);
    const ex = React.useRef(null);
```
② **가져오고 싶은 input 박스에 ref 넣어준다**
```javascript
<input type="text" size="60" maxlength='15' ref={word} />
<input type="text" size="60" maxlength='50' ref={mean} />
<input type="text" size="60" maxlength='50' ref={ex} />  
```

⭐ **전체 코드** ⭐
```javascript
import styled from 'styled-components';
import React from "react";
import { useHistory } from 'react-router-dom';
import { useDispatch } from "react-redux"; 
import { createWordFB } from "../redux/modules/words"; // 액션생성함수
import Header from "./Header";

const Add = () => {
    const history = useHistory();
    const dispatch = useDispatch();

    const word = React.useRef(null);
    const mean = React.useRef(null);
    const ex = React.useRef(null);

    const addWordList = () => {
        dispatch(createWordFB({
            word : word.current.value,
            mean : mean.current.value,
            ex : ex.current.value,
        }));
        history.goBack();
      };

    return (    
        <AddWrap>
            <Header />
            <Content>
                <Title>단어 추가하기</Title>
                <Word>
                    <label>단어</label> <br/>
                    <input type="text" size="60" maxlength='15' ref={word} />
                </Word>
                <Mean>
                    <label>설명</label> <br/>
                    <input type="text" size="60" maxlength='50' ref={mean} />
                </Mean>
                <Ex>
                    <label>예시</label> <br/>
                    <input type="text" size="60" maxlength='50' ref={ex} />
                </Ex>
                <Button onClick={addWordList}>
                    저장하기
                </Button>
            </Content>
        </AddWrap>

    )
}
```
<br/>

 - **게시글 목록을 리덕스에서 관리하기** 
 - **작성한 게시글을 리덕스 내 게시글 목록에 추가하기**
 
① **액션 타입** 
```javascript
const CREATE = 'words/CREATE';
```

② **액션 생성 함수**
```javascript
export function createWord(word) {
  return { type: CREATE, word }; // word : 액션객체
} 
```

③ **초기값 설정**
```javascript
const initialState = {
    list: [], // 빈 배열
  };
```

④ **리듀서 함수**
```javascript
export default function reducer(state = initialState, action = {}) {
    switch (action.type) {
      case "words/CREATE": {
        const new_word_list = [...state.list, action.word]; // 원래 있던 list배열에 새로 들어온 데이터 word(액션 객체)를 넣는다.
        return { list: new_word_list }; // 객체 형식으로 넣어서 반환
      }
      default:
      	return state;
    }
  }   
```

⑤ **input박스에서 입력된 값 가져오기**
```javascript
const dispatch = useDispatch(); 

const addWordList = () => {
        dispatch(createWord({ // 액션을 dispatch
            word : word.current.value, 
            mean : mean.current.value,
            ex : ex.current.value,
        }));
        history.goBack();
      };
```

⭐ **전체 코드** ⭐
```javascript
const CREATE = 'words/CREATE';

const initialState = {
    list: [],
  };

export function createWord(word) {
  return { type: CREATE, word };
}

export default function reducer(state = initialState, action = {}) {
    switch (action.type) {
      case "words/CREATE": {
        const new_word_list = [...state.list, action.word];
        console.log('6 리듀서', new_word_list);
        return { list: new_word_list };
      }
      default:
      	return state;
    }
  }        
```

## 오늘 느낀점
많이 어려운데 계속 반복해서 보고 또 보니까 이제는 어떤식으로 Redux가 작동하는지 감이 잡힌다. 아직은 내가 생각한 것들을 주르륵 코드로 작성할 순 없지만 이렇게 반복하다보면은 액션 타입, 액션 생성함수, 리듀서 정도는 착착 쓰지 않을까 싶다. 부족한 부분은 본인 스스로가 제일 잘 알기 때문에 잊지 않고 열심히 공부 하기!